# API 通知投递系统设计文档

> 本文档是系统设计与决策说明。代码实现、运行方式和接口示例见 [README.md](README.md)；AI 使用说明见 [AI使用文档.md](AI使用文档.md)。

## 1. 作业目标与问题理解

企业内部多个业务系统在关键业务事件发生后，需要通知外部供应商提供的 HTTP(S) API。例如：

- 广告平台引流注册成功后，向广告平台回传注册事件；
- 订阅付款成功后，通知 CRM 更新 Contact 状态；
- 商品购买成功后，通知库存系统扣减库存。

不同供应商的 URL、Header、Body 和响应语义可能不同。业务系统不需要同步等待供应商的业务结果，但需要确保通知请求不会因为一次网络失败或服务重启而轻易丢失。

本系统的核心不是简单的 HTTP 转发，而是提供一个统一的通知接入和可靠投递能力：

1. 以规定好的协议接收内部业务系统提交的通知任务；
2. 在任务持久化成功后返回接收确认；
3. 由后台 Worker 异步调用对应的供应商适配器；
4. 根据外部调用结果进行成功、最终失败或结果未知处理；
5. 持久化通知状态，使任务可查询、可排查、可重放。

## 2. MVP 设计结论

第一版采用单实例服务，使用 SQLite 保存通知任务，并在同一进程中运行持久化 Worker。

```text
业务系统
    |
    | gRPC SubmitNotification
    v
Notification Service
    |
    | 校验、幂等、持久化
    v
SQLite
    |
    | Worker 拉取任务
    v
Provider Adapter
    |
    | HTTP(S)
    v
外部供应商 API
```

gRPC 用于定义内部接入协议和服务边界；可靠性由持久化、任务状态机、Worker 和幂等机制共同提供。

如果需要更通用好访问的 HTTP 客户端，可以在 gRPC 服务上增加 grpc-gateway，将 HTTP `POST` / `GET` 映射到 gRPC RPC；内部核心契约仍然是 protobuf/gRPC。

## 3. 系统边界

### 3.1 系统负责

- 提供统一的 gRPC 通知提交接口，也就是统一触发方式；
- 校验通知请求和供应商标识；
- 生成通知 ID，并处理幂等键；
- 持久化待投递任务；
- 选择并调用对应的 Provider Adapter；
- 判断外部请求成功、临时失败、永久失败或结果未知；
- 当前不自动重试，投递失败的任务进入最终失败状态并记录最近错误；
- 保存任务状态、尝试次数和最近错误；
- 提供通知状态查询；
- 对最终失败任务保留人工重放的可能性,不过这个需要按不同供应商提供的不同接口设计。

### 3.2 系统不负责

- 判断业务事件本身是否正确；
- 处理供应商内部的业务事务；
- 保证供应商一直可用；
- 保证 exactly-once 投递；
- 保证 HTTP 2xx 后供应商内部业务一定执行成功；
- 在 MVP 中实现完整的供应商配置后台；
- 在 MVP 中引入复杂的分布式调度、消息编排或运营平台。

## 4. 接口契约

### 4.1 gRPC API

两个核心 RPC：

```protobuf
service NotificationService {
  rpc SubmitNotification(SubmitNotificationRequest)
      returns (SubmitNotificationResponse);

  rpc GetNotification(GetNotificationRequest)
      returns (GetNotificationResponse);
}
```

提交请求使用统一事件模型，不让业务方直接感知供应商 HTTP 细节：

```protobuf
message SubmitNotificationRequest {
  string provider = 1;
  string event_type = 2;
  string idempotency_key = 3;
  bytes payload = 4;
}
```

`payload` 在 MVP 中可以使用 JSON 字节；供应商 URL、Header 和 Body 格式由服务端的 Adapter 决定。

### 4.2 接收成功语义

`SubmitNotification` 返回 `ACCEPTED` 的前提是：

1. 请求格式合法；
2. provider 对应的 Adapter 存在；
3. 通知任务已经成功提交到数据库事务。

此时返回：

```text
notification_id = <generated-id>
status = ACCEPTED
```

`ACCEPTED` 不表示外部供应商已经收到，只表示本服务已经可靠接收该任务。

以下情况不返回接收成功：

- 参数非法；
- 供应商不存在；
- 幂等键与已有任务内容冲突；
- 数据库不可用或事务提交失败。

### 4.3 HTTP 映射

如果启用 grpc-gateway，可以提供：

```text
POST /v1/notifications
GET  /v1/notifications/{id}
```

HTTP 是外部表现形式，内部仍通过统一的 protobuf 请求进入 service 层。

## 5. 分层与职责

### API / Service

负责解析 protobuf、完成基础参数校验、转换领域对象并调用 Domain。Service 不负责 HTTP 供应商调用，也不负责投递结果分类。

### Domain

负责通知系统本身的业务规则：

- 创建通知任务；
- 幂等判断；
- 状态流转；
- 将投递结果映射为成功或最终失败；
- 记录最近错误、投递时间和尝试次数。

Domain 不依赖 protobuf、HTTP 状态码或具体供应商 JSON 格式。

### Datasource

负责 SQLite 持久化和读取：

- 创建任务；
- 查询任务；
- 原子领取待投递任务；
- 更新任务状态；
- 保存尝试次数、计划投递时间、租约和错误信息。

### Registry

负责在进程启动时统一打开 SQLite、组装 Provider Adapter，并向 Service 与 Worker 提供共享依赖；Registry 同时负责关闭 SQLite 等运行时资源。

### Worker

负责异步执行：

1. 查找到期的待投递任务；
2. 原子领取任务；
3. 调用 Adapter；
4. 将 Adapter 的结果交给 Domain；
5. 持久化新的任务状态。

### Adapter

负责屏蔽供应商之间的协议差异：

- 构造 URL；
- 组装 Header；
- 将统一 payload 转换为供应商 Body；
- 调用 HTTP(S)；
- 解析供应商响应；
- 将结果转换为统一的投递结果。

Adapter 不负责整个通知生命周期，只负责“如何与某个供应商通信”。

## 6. 任务状态与可靠性语义

### 6.1 状态

```text
PENDING       已持久化，等待投递
DELIVERING    已被 Worker 领取，正在投递
SUCCEEDED     外部 HTTP 投递成功
DEAD          投递失败（永久失败、临时失败或结果未知），当前不自动重试
```

### 6.2 成功与失败

建议将外部 HTTP 2xx 视为 HTTP 层投递成功。

### 6.3 重试策略

当前 MVP 不实现自动重试。原因是实习和实际项目里经常遇到这样的场景：数据库网络抖动导致投递失败时，如果大量任务同时进入重试，会产生一批新的并发请求，反而容易把数据库或下游再次压垮。所以本版本选择失败后记录错误日志和 `last_error`，由开发人员通过状态查询排查；确实需要补投的场景留到后续人工重放接口处理。

如果未来需要自动重试，应由 Domain 进行逻辑编排，驱动 Worker 执行。Adapter 只将供应商结果归一化为：

```text
Succeeded
Retryable
Permanent
Unknown
```

`Unknown` 表示无法确认外部是否已经处理，例如请求已经发出但响应丢失。当前 MVP 对 `Unknown` 也不自动重试，而是进入 `DEAD` 并保留最近错误，避免重复投递造成业务副作用。

这样可以避免每个 Adapter 重复实现一套投递结果分类逻辑。

## 7. 数据模型

MVP 可以使用一张核心表：

```text
notifications
- id
- provider
- event_type
- payload
- payload_hash
- idempotency_key
- status
- attempts
- next_attempt_at
- lease_until
- last_error
- created_at
- updated_at
- delivered_at
```

`idempotency_key` 建立唯一约束，实际约束作用域为 `(provider, idempotency_key)`。`payload_hash` 保存 `payload` 的摘要，用于判断同一幂等键重复提交时内容是否一致：内容一致返回已有任务，内容不同返回冲突。

`lease_until` 用于防止 Worker 在崩溃后永久占用任务。超过租约时间的 `DELIVERING` 任务可以重新进入投递流程。

`next_attempt_at` 用于标记计划投递时间；当前版本不自动重试，失败任务会把它置空。

## 8. Provider Adapter 设计

建议定义统一接口：

```go
type Adapter interface {
    Deliver(ctx context.Context, notification Notification) DeliveryResult
}
```

推荐目录：

```text
internal/adapter/
├── adapter.go
├── crm/
│   └── adapter.go
├── inventory/
│   └── adapter.go
└── webhook/
    └── adapter.go
```

供应商是外部协议差异，不需要复制完整的 Domain、Datasource 和 RPC 入口。所有供应商共享通知任务的生命周期和投递结果分类框架。

如果某个供应商后续具备独立部署、独立扩缩容或独立团队维护的必要，再考虑把该 Adapter 演进成独立 gRPC 服务。

## 9. 建议目录结构

```text
notification/
├── cmd/
│   └── notification/
│       └── main.go
├── api/
│   └── pb/
│       ├── server.proto
│       ├── server.pb.go
│       └── server_grpc.pb.go
├── internal/
│   ├── service/
│   │   └── grpc.go
│   ├── domain/
│   │   └── notification/
│   │       ├── domain.go
│   │       ├── model.go
│   │       ├── submit.go
│   │       ├── delivery.go
│   │       └── store.go
│   ├── datasource/
│   │   └── notification/
│   │       ├── sqlite.go
│   │       └── repository.go
│   ├── adapter/
│   │   ├── adapter.go
│   │   ├── crm/
│   │   ├── inventory/
│   │   └── webhook/
│   ├── worker/
│   │   └── delivery.go
│   └── registry/
│       └── registry.go
├── migrations/
│   └── 001_create_notifications.sql
├── tests/
│   └── e2e/
├── README.md
├── 设计文档.md
└── AI使用文档.md
```

该结构参考了个人知识库中 Go/gRPC 项目的 `api/pb`、`internal/service`、`internal/domain`、`internal/datasource`、`internal/registry` 分层约定，不依赖公司内部工具。开源仓库应优先使用标准 `protoc` 或 `buf` 生成 protobuf 代码。

## 10. 取舍与演进

### 为什么选择 SQLite + 单实例 Worker

- 不需要额外部署数据库或消息队列；
- 可以验证任务持久化、服务重启恢复、最终失败落库和租约恢复语义；
- 代码和运行方式简单，适合 MVP；
- 便于通过自动化测试复现失败场景。

### 为什么不直接使用 Kafka、Redis Streams 或 Temporal

这些组件可以提供更强的吞吐、调度和编排能力，但会引入额外的部署、运维和学习成本。当前作业关注的是边界、可靠性语义和工程取舍，第一版没有必要先解决大规模分布式问题。

### 后续演进方向

1. SQLite 迁移到 PostgreSQL；
2. 使用多个 Worker，并通过数据库锁或队列实现并发领取，不过并发的话对于负载也比较重，所以可能是单个服务有单个 worker 或者两个 worker 去做就可以了；
3. 增加 dead-letter 查询和人工重放接口；
4. 增加 Prometheus 指标、结构化日志和告警，这个就能实现有错误的监控；
5. 对不同 provider/event_type 配置不同的失败策略；
6. 当 Adapter 具备独立部署价值时，再拆分为独立 gRPC 服务；
7. 业务方需要事务一致性时，引入 Outbox 或事务消息。

## 11. 测试与验收标准

MVP 至少验证：

- 合法请求可以通过 gRPC 提交并持久化；
- 数据库写入失败时不会返回 `ACCEPTED`；
- 重复幂等键不会产生重复任务；
- Worker 可以从 SQLite 拉取任务并调用 Adapter；
- 外部服务返回 2xx 时，任务最终为 `SUCCEEDED`；
- 外部服务返回 5xx、网络失败或 `Unknown` 时，不进入重试等待，任务进入 `DEAD` 并记录最近错误；
- 明确的 4xx 会进入 `DEAD`；
- Worker 或进程重启后，未完成任务仍可以继续投递；
- 两个 Worker 不会同时处理同一租约内的任务；
- 可以通过 `GetNotification` 查询任务状态和最近错误。

## 12. 实现落地状态

设计已在当前仓库落地为可运行 MVP，具体实现、运行方式、接口示例和测试覆盖见 [README.md](README.md)。

## 13. 重构设想：业务模块化（方向，尚未编码）

> 本节记录 2026-08-19 提出的重构方向，仅整理为设计原则与目标结构，本轮不涉及代码改动。交互过程见 [AI使用文档.md](AI使用文档.md) 第 4.3 节。

### 13.1 动机：从水平分层到业务模块化

当前结构按技术职责横切：所有 Adapter 在 `internal/adapter/`，通知领域逻辑在 `internal/domain/notification/`，持久化在 `internal/datasource/notification/`，全局组装在 `internal/registry/`。业务变多后会出现几个问题：

- 每个新业务都要在全局各层里分散加代码，业务职责难以一眼定位；
- 全局 Registry 需要认识所有业务，模块间依赖边界不清晰；
- 跨业务协作（例如投递成功后触发 CRM 更新、库存扣减等多个副作用）没有明确的契约和编排位置。

重构目标：按「小业务」纵向切分。一个小业务 = 一个独立文件夹，自包含 Registry、Domain、DataSource 与 RPC 入口；业务之间只通过 RPC 契约协作。

### 13.2 八条设想与落地解读

原设想逐条对应到设计：

1. **每个小业务封装在独立小文件夹**：例如 `internal/notification/`、`internal/adapter/`。文件夹是业务边界，可独立阅读、独立测试、独立演进。
2. **文件夹内包含 Registry**：Registry 是该业务的组装入口，负责创建 Domain、注入 DataSource、注册 Provider、注册对外 RPC；其他业务只通过它提供的接口或客户端协作。
3. **包含 Domain**：复杂业务逻辑放 Domain；Domain 保持纯逻辑，不碰 SQL、HTTP、protobuf，只依赖接口。
4. **Domain 注册 DataSource 与 RPC 调用**：本模块持久化通过 DataSource 接口注入；跨模块协作通过对方业务的 RPC，不在 Domain 里直接 import 对方实现。
5. **子文件夹代表不同业务提供商**：例如 `internal/adapter/providers/` 下的 `webhook/`、`crm/`、`inventory/`。提供商只实现协议差异，通过 Registry 注册，不复制模块的 Domain/DataSource。
6. **跨业务通过 RPC**：投递的多个副作用（CRM 更新、库存扣减等）各自是独立业务；编排方按顺序调用各业务 RPC 并记录结果。单进程阶段 RPC 先做进程内实现，保留契约边界。
7. **DataSource 单表**：一个 DataSource 只对应一张表，表所有权明确；跨表读写、聚合查询不放进 DataSource。
8. **整体节奏**：文档先行、代码后动；先固化方向，再按模块逐步迁移。

### 13.3 目标目录结构（示意）

```text
internal/
├── notification/                 # 小业务：通知
│   ├── registry.go               # 模块 Registry：组装 Domain / DataSource / RPC
│   ├── domain/                   # 状态机、幂等、投递结果分类
│   ├── datasource/               # 只读写 notifications 一张表
│   └── rpc/                      # 对外 RPC：SubmitNotification / GetNotification
├── adapter/                      # 小业务：对外投递
│   ├── registry.go               # 模块 Registry
│   ├── domain/                   # 投递规则与副作用编排
│   ├── datasource/               # 只读写投递记录一张表
│   ├── rpc/                      # 供其他业务调用的投递 RPC
│   └── providers/                # 业务提供商
│       ├── webhook/              # Webhook 提供商
│       ├── crm/                  # CRM 提供商
│       └── inventory/            # 库存提供商
├── worker/                       # 调度执行（可归属通知模块，落地时确认）
└── app/                          # 顶层组装：组合各模块 Registry（替代现有全局 Registry）
```

注：`worker/` 归为公共调度还是收进 `notification/`，取决于它是否只服务一个业务，落地时确认；当前代码保持不变。

### 13.4 关键取舍

- **RPC 是边界，不一定是网络**：先在进程内实现 RPC 契约（接口调用），避免过早引入网络调用、超时、重试和服务发现成本；未来某业务需要独立部署时，把接口实现换成 gRPC Client 即可。
- **提供商不复制完整模块**：提供商共享模块的 Domain、DataSource 和 Registry，只注册协议差异；独立拆分留给真正的部署或团队边界。
- **副作用必须有人编排**：每个副作用调用都定义成功、失败、超时语义并落库，编排方决定继续、停止或补偿，避免“投递成功但副作用只做了一半”。
- **单表约束下的查询**：跨表聚合、报表不交给 DataSource，另建只读查询层或视图。

### 13.5 建议落地顺序

1. 文档先行（本轮）；
2. 将现有 Adapter 收拢为独立模块：Registry / Domain / DataSource / RPC 契约；
3. 定义跨业务 RPC 契约与副作用编排，以“投递成功后多个副作用”为第一个场景；
4. 按需新增 `crm/`、`inventory/` 等提供商子目录；
5. 保持现有接口、测试与 SQLite 存储不破坏，逐步迁移。
